Week Planner user files
=======================

This directory stores data generated by the Week Planner add-on.

- `plan.json` keeps the canonical list of planned decks (`did`, `iso`, `order`).
- You can back up or version-control this folder safely.
- Deleting the folder will reset the planner to the configuration defaults.

